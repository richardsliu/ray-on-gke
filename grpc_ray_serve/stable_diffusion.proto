syntax = "proto3";

// Serivce that is only used to test GrpcServer
message StableDiffusionRequest {
  string prompt = 1;
}

message StableDiffusionResponse {
  bytes contents = 1;
}


service StableDiffusionService {
  rpc Generate(StableDiffusionRequest) returns (StableDiffusionResponse);
}
